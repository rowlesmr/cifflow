[package]
name = "pycifparse_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "pycifparse_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
arrow = { version = "54", features = ["ipc", "pyarrow"] }
